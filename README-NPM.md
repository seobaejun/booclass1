# npm (선택)

브라우저에서는 `js/mime-types-browser.js`로 PDF·엑셀·텍스트 등 확장자별 **Content-Type**을 지정합니다.  
Node/번들러에서 `mime-types` 패키지와 동일한 목적이 필요할 때만 설치하세요.

```bash
cd boostclass
npm install
```

실제 페이지 동작에는 **npm 설치가 필수는 아닙니다.**
